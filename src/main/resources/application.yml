spring:
  application:
    name: Ingrate
  profiles:
    include: db
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
server:
  port: 8080